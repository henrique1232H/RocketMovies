import styled from "styled-components";

export const ResponsiveScale = styled.div`
    width: min(129rem, 90%);
    margin: 5rem auto 0 auto;
`

export const SrOnly = styled.label`
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border-width: 0;
`